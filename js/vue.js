// Re-exports the Vue 3 browser-ESM build (includes the template compiler)
// from one place, so every component does `import { ... } from '../vue.js'`
// instead of repeating the CDN URL. Change the version/URL here only.
export * from 'https://unpkg.com/vue@3.4.21/dist/vue.esm-browser.js';
